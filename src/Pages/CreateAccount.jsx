import React, { useState } from "react";
import { Outlet } from "react-router-dom";
const CreateAccount = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default CreateAccount;
